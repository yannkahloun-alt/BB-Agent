name: Issue 23 hard coverage fix

on:
  push:
    branches: [codex/issue-23-validation-harness]

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/issue-23-validation-harness
          fetch-depth: 0
          submodules: recursive
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python tools/verify_workflow_dependency.py
      - run: python -m pip install -e ".[dev]"
      - run: python tools/issue23_hard_coverage_fix.py
      - run: python -m ruff format src/bb_agent/validation.py tests/test_validation.py
      - run: python -m ruff check src/bb_agent/validation.py tests/test_validation.py
      - run: python -m pytest tests/test_validation.py
      - name: Commit and self-clean
        run: |
          rm tools/issue23_hard_coverage_fix.py .github/workflows/issue23-hard-coverage-fix.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/bb_agent/validation.py tests/test_validation.py docs/VALIDATION.md tools/issue23_hard_coverage_fix.py .github/workflows/issue23-hard-coverage-fix.yml
          git commit -m "Keep #23 coverage correctness hard on calibration fixtures"
          git push origin HEAD:codex/issue-23-validation-harness
